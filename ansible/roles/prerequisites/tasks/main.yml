---
- name: Check required host commands
  ansible.builtin.command: "{{ item }} --version"
  loop:
    - distrobox
    - "{{ ads_container_manager }}"
  changed_when: false

- name: Validate sandbox inputs
  ansible.builtin.assert:
    that:
      - ads_box_name is match('^[A-Za-z0-9][A-Za-z0-9_.-]*$')
      - ads_image | length > 0
      - ads_box_home is match('^/')
      - ads_box_home != '/'
      - ads_box_home != lookup('env', 'HOME')
      - ads_container_pids_limit | int >= 64
      - ads_container_memory is match('^[1-9][0-9]*[kKmMgG]$')
      - ads_container_cpus | float > 0
      - ads_container_allow_host_loopback or ads_container_manager == 'podman'
      - ads_ai_jail_deny_paths | length > 0
      - "'/run/host' in ads_ai_jail_deny_paths"
      - "'/usr/bin/distrobox-host-exec' in ads_ai_jail_deny_paths"
      - ads_enabled_harnesses | difference(ads_harness_catalog.keys() | list) | length == 0
      - ads_buzz_agent_allowlist.keys() | difference(ads_enabled_harnesses) | length == 0
      - not ads_repositories_mount_enabled or ads_repositories_host_path is match('^/')
      - not ads_repositories_mount_enabled or ads_repositories_box_path is match('^/')
      - not ads_repositories_mount_enabled or ads_repositories_host_path not in ['/', lookup('env', 'HOME'), ads_box_home]
      - not ads_repositories_mount_enabled or ads_repositories_box_path not in ['/', '/home', '/run', '/run/host']
    fail_msg: Check the ads_* values in host_vars/localhost.yml.

- name: Check the optional repository source
  ansible.builtin.stat:
    path: "{{ ads_repositories_host_path }}"
  register: ads_repo_source
  when: ads_repositories_mount_enabled

- name: Validate the optional repository source is a directory
  ansible.builtin.assert:
    that: ads_repo_source.stat.exists and ads_repo_source.stat.isdir
    fail_msg: "Repository mount source must be an existing directory: {{ ads_repositories_host_path }}"
  when: ads_repositories_mount_enabled

- name: Validate Buzz agent allowlist entries
  ansible.builtin.assert:
    that:
      - item.key is match('^[A-Za-z0-9][A-Za-z0-9_.-]*$')
      - item.value.command is match('^[A-Za-z0-9][A-Za-z0-9_.+-]*$')
      - item.value.command == ads_harness_catalog[item.key].command
      - item.value.args is sequence
      - item.value.args | select('search', ',') | list | length == 0
    fail_msg: >-
      Buzz agent allowlist entries need safe names, a bare executable, and fixed
      arguments without commas (BUZZ_ACP_AGENT_ARGS is comma-delimited).
  loop: "{{ ads_buzz_agent_allowlist | dict2items }}"

- name: Validate mandatory ai-jail deny paths
  ansible.builtin.assert:
    that:
      - item is match('^/')
      - item != '/'
    fail_msg: ai-jail deny paths must be absolute, narrow paths and cannot be root.
  loop: "{{ ads_ai_jail_deny_paths }}"
